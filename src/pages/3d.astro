---
import Layout from '../layouts/layout.astro';
import Navbar from '../components/Navbar.astro';
import Contacto from '../components/Contacto.astro';
---

<Layout>
    <Navbar />
    
    <!-- Cabecera de Diseño 3D -->
    <section class="pt-40 pb-12 px-5 text-center relative z-10">
        <h1 class="text-5xl md:text-7xl font-extrabold mb-4">
            <span class="bg-gradient-to-r from-[#00ff66] via-[#00ccff] to-[#00ff66] bg-[length:200%_auto] text-transparent bg-clip-text animate-[gradientFlow_6s_infinite_linear]">
                Diseño 3D
            </span>
        </h1>
        <p class="text-[#cbd5e1] text-lg max-w-2xl mx-auto">
            Modelado, rigging, texturizado y optimización de assets. Flujos de trabajo orientados a la implementación técnica en motores de videojuegos.
        </p>
    </section>

    <!-- Galería 3D -->
    <section class="py-10 px-5 relative z-10">
        <!-- Hacemos el contenedor elástico hasta los 4000px -->
        <div class="w-full max-w-[4000px] mx-auto">
            <!-- columns-[300px] crea automáticamente las columnas que quepan según el ancho/zoom -->
            <div class="columns-1 sm:columns-[250px] md:columns-[300px] gap-6 space-y-6">
                
                <!-- Tarjeta 1 (WEBM/MP4) -->
                <div class="lightbox-trigger relative group rounded-2xl overflow-hidden border border-white/10 bg-[#0a0508] hover:border-[#00ff66]/50 transition-all duration-300 break-inside-avoid cursor-pointer" data-src="/3D/ComicMat.webm">
                    <!-- Es vital incluir autoplay, loop, muted y playsinline para que actúe como un GIF -->
                    <video src="/3D/ComicMat.webm" autoplay loop muted playsinline class="w-full h-auto object-cover group-hover:scale-105 transition-transform duration-500"></video>
                    <div class="absolute inset-0 bg-[#0a0508]/80 opacity-0 group-hover:opacity-100 transition-opacity duration-300 flex flex-col justify-end p-6 pointer-events-none">
                        <h3 class="text-xl font-bold text-white mb-1">Material estilo comic</h3>
                        <p class="text-[#cbd5e1] text-sm">Materiales</p>
                    </div>
                </div>

                <!-- Tarjeta 2 (WEBM/MP4) -->
                <div class="lightbox-trigger relative group rounded-2xl overflow-hidden border border-white/10 bg-[#0a0508] hover:border-[#00ff66]/50 transition-all duration-300 break-inside-avoid cursor-pointer" data-src="/3D/FrogCrystal.webm">
                    <!-- Es vital incluir autoplay, loop, muted y playsinline para que actúe como un GIF -->
                    <video src="/3D/FrogCrystal.webm" autoplay loop muted playsinline class="w-full h-auto object-cover group-hover:scale-105 transition-transform duration-500"></video>
                    <div class="absolute inset-0 bg-[#0a0508]/80 opacity-0 group-hover:opacity-100 transition-opacity duration-300 flex flex-col justify-end p-6 pointer-events-none">
                        <h3 class="text-xl font-bold text-white mb-1">Material estilo cristal</h3>
                        <p class="text-[#cbd5e1] text-sm">Materiales + Modelado</p>
                    </div>
                </div>

                <!-- Tarjeta 3 (WEBM/MP4) -->
                <div class="lightbox-trigger relative group rounded-2xl overflow-hidden border border-white/10 bg-[#0a0508] hover:border-[#00ff66]/50 transition-all duration-300 break-inside-avoid cursor-pointer" data-src="/3D/Fire.webm">
                    <!-- Es vital incluir autoplay, loop, muted y playsinline para que actúe como un GIF -->
                    <video src="/3D/Fire.webm" autoplay loop muted playsinline class="w-full h-auto object-cover group-hover:scale-105 transition-transform duration-500"></video>
                    <div class="absolute inset-0 bg-[#0a0508]/80 opacity-0 group-hover:opacity-100 transition-opacity duration-300 flex flex-col justify-end p-6 pointer-events-none">
                        <h3 class="text-xl font-bold text-white mb-1">Animación y VFX</h3>
                        <p class="text-[#cbd5e1] text-sm">VFX</p>
                    </div>
                </div>

                <!-- Tarjeta 4 (WEBM/MP4) -->
                <div class="lightbox-trigger relative group rounded-2xl overflow-hidden border border-white/10 bg-[#0a0508] hover:border-[#00ff66]/50 transition-all duration-300 break-inside-avoid cursor-pointer" data-src="/3D/Flower2.webm">
                    <!-- Es vital incluir autoplay, loop, muted y playsinline para que actúe como un GIF -->
                    <video src="/3D/Flower2.webm" autoplay loop muted playsinline class="w-full h-auto object-cover group-hover:scale-105 transition-transform duration-500"></video>
                    <div class="absolute inset-0 bg-[#0a0508]/80 opacity-0 group-hover:opacity-100 transition-opacity duration-300 flex flex-col justify-end p-6 pointer-events-none">
                        <h3 class="text-xl font-bold text-white mb-1">Animación y Nodos de Geometría</h3>
                        <p class="text-[#cbd5e1] text-sm">Procedural</p>
                    </div>
                </div>

                <!-- Tarjeta 5 (WEBM/MP4) -->
                <div class="lightbox-trigger relative group rounded-2xl overflow-hidden border border-white/10 bg-[#0a0508] hover:border-[#00ff66]/50 transition-all duration-300 break-inside-avoid cursor-pointer" data-src="/3D/Kato.mp4">
                    <!-- Es vital incluir autoplay, loop, muted y playsinline para que actúe como un GIF -->
                    <video src="/3D/Kato.mp4" autoplay loop muted playsinline class="w-full h-auto object-cover group-hover:scale-105 transition-transform duration-500"></video>
                    <div class="absolute inset-0 bg-[#0a0508]/80 opacity-0 group-hover:opacity-100 transition-opacity duration-300 flex flex-col justify-end p-6 pointer-events-none">
                        <h3 class="text-xl font-bold text-white mb-1">Animación de Personaje Idle</h3>
                        <p class="text-[#cbd5e1] text-sm">Personaje</p>
                    </div>
                </div>

                <!-- Tarjeta 6 (WEBM/MP4) -->
                <div class="lightbox-trigger relative group rounded-2xl overflow-hidden border border-white/10 bg-[#0a0508] hover:border-[#00ff66]/50 transition-all duration-300 break-inside-avoid cursor-pointer" data-src="/3D/KatoE.mp4">
                    <!-- Es vital incluir autoplay, loop, muted y playsinline para que actúe como un GIF -->
                    <video src="/3D/KatoE.mp4" autoplay loop muted playsinline class="w-full h-auto object-cover group-hover:scale-105 transition-transform duration-500"></video>
                    <div class="absolute inset-0 bg-[#0a0508]/80 opacity-0 group-hover:opacity-100 transition-opacity duration-300 flex flex-col justify-end p-6 pointer-events-none">
                        <h3 class="text-xl font-bold text-white mb-1">Animación de Personaje</h3>
                        <p class="text-[#cbd5e1] text-sm">Personaje</p>
                    </div>
                </div>

                <!-- Tarjeta 7 (PNG/JPG) -->
                <div class="lightbox-trigger relative group rounded-2xl overflow-hidden border border-white/10 bg-[#0a0508] hover:border-[#00ff66]/50 transition-all duration-300 break-inside-avoid cursor-pointer" data-src="/3D/KatoJumpscare.gif">
                    <img src="/3D/KatoJumpscare.gif" alt="KatoJumpscare" class="w-full h-auto object-cover group-hover:scale-105 transition-transform duration-500" />
                    <div class="absolute inset-0 bg-[#0a0508]/80 opacity-0 group-hover:opacity-100 transition-opacity duration-300 flex flex-col justify-end p-6 pointer-events-none">
                        <h3 class="text-xl font-bold text-white mb-1">Animación de Personaje Correr</h3>
                        <p class="text-[#cbd5e1] text-sm">Personaje</p>
                    </div>
                </div>

                <!-- Tarjeta 8 (PNG/JPG) -->
                <div class="lightbox-trigger relative group rounded-2xl overflow-hidden border border-white/10 bg-[#0a0508] hover:border-[#00ff66]/50 transition-all duration-300 break-inside-avoid cursor-pointer" data-src="/3D/Lego.png">
                    <img src="/3D/Lego.png" alt="Lego" class="w-full h-auto object-cover group-hover:scale-105 transition-transform duration-500" />
                    <div class="absolute inset-0 bg-[#0a0508]/80 opacity-0 group-hover:opacity-100 transition-opacity duration-300 flex flex-col justify-end p-6 pointer-events-none">
                        <h3 class="text-xl font-bold text-white mb-1">Primeros modelos</h3>
                        <p class="text-[#cbd5e1] text-sm">Modelado</p>
                    </div>
                </div>

                <!-- Tarjeta 9 (WEBM/MP4) -->
                <div class="lightbox-trigger relative group rounded-2xl overflow-hidden border border-white/10 bg-[#0a0508] hover:border-[#00ff66]/50 transition-all duration-300 break-inside-avoid cursor-pointer" data-src="/3D/ProceduralBuilding.webm">
                    <!-- Es vital incluir autoplay, loop, muted y playsinline para que actúe como un GIF -->
                    <video src="/3D/ProceduralBuilding.webm" autoplay loop muted playsinline class="w-full h-auto object-cover group-hover:scale-105 transition-transform duration-500"></video>
                    <div class="absolute inset-0 bg-[#0a0508]/80 opacity-0 group-hover:opacity-100 transition-opacity duration-300 flex flex-col justify-end p-6 pointer-events-none">
                        <h3 class="text-xl font-bold text-white mb-1">Animación y Nodos de Geometría</h3>
                        <p class="text-[#cbd5e1] text-sm">Procedural</p>
                    </div>
                </div>

                <!-- Tarjeta 10 (WEBM/MP4) -->
                <div class="lightbox-trigger relative group rounded-2xl overflow-hidden border border-white/10 bg-[#0a0508] hover:border-[#00ff66]/50 transition-all duration-300 break-inside-avoid cursor-pointer" data-src="/3D/ProceduralFlowerGoofy.webm">
                    <!-- Es vital incluir autoplay, loop, muted y playsinline para que actúe como un GIF -->
                    <video src="/3D/ProceduralFlowerGoofy.webm" autoplay loop muted playsinline class="w-full h-auto object-cover group-hover:scale-105 transition-transform duration-500"></video>
                    <div class="absolute inset-0 bg-[#0a0508]/80 opacity-0 group-hover:opacity-100 transition-opacity duration-300 flex flex-col justify-end p-6 pointer-events-none">
                        <h3 class="text-xl font-bold text-white mb-1">Animación y Nodos de Geometría</h3>
                        <p class="text-[#cbd5e1] text-sm">Procedural</p>
                    </div>
                </div>

                <!-- Tarjeta 11 (WEBM/MP4) -->
                <div class="lightbox-trigger relative group rounded-2xl overflow-hidden border border-white/10 bg-[#0a0508] hover:border-[#00ff66]/50 transition-all duration-300 break-inside-avoid cursor-pointer" data-src="/3D/Projectile.webm">
                    <!-- Es vital incluir autoplay, loop, muted y playsinline para que actúe como un GIF -->
                    <video src="/3D/Projectile.webm" autoplay loop muted playsinline class="w-full h-auto object-cover group-hover:scale-105 transition-transform duration-500"></video>
                    <div class="absolute inset-0 bg-[#0a0508]/80 opacity-0 group-hover:opacity-100 transition-opacity duration-300 flex flex-col justify-end p-6 pointer-events-none">
                        <h3 class="text-xl font-bold text-white mb-1">Animación y VFX</h3>
                        <p class="text-[#cbd5e1] text-sm">VFX</p>
                    </div>
                </div>

                <!-- Tarjeta 12 (PNG/JPG) -->
                <div class="lightbox-trigger relative group rounded-2xl overflow-hidden border border-white/10 bg-[#0a0508] hover:border-[#00ff66]/50 transition-all duration-300 break-inside-avoid cursor-pointer" data-src="/3D/Pyra1.png">
                    <img src="/3D/Pyra1.png" alt="Pyra" class="w-full h-auto object-cover group-hover:scale-105 transition-transform duration-500" />
                    <div class="absolute inset-0 bg-[#0a0508]/80 opacity-0 group-hover:opacity-100 transition-opacity duration-300 flex flex-col justify-end p-6 pointer-events-none">
                        <h3 class="text-xl font-bold text-white mb-1">Personaje estilo anime</h3>
                        <p class="text-[#cbd5e1] text-sm">Modelado</p>
                    </div>
                </div>

                <!-- Tarjeta 13 (PNG/JPG) -->
                <div class="lightbox-trigger relative group rounded-2xl overflow-hidden border border-white/10 bg-[#0a0508] hover:border-[#00ff66]/50 transition-all duration-300 break-inside-avoid cursor-pointer" data-src="/3D/Pyra2.png">
                    <img src="/3D/Pyra2.png" alt="Pyra" class="w-full h-auto object-cover group-hover:scale-105 transition-transform duration-500" />
                    <div class="absolute inset-0 bg-[#0a0508]/80 opacity-0 group-hover:opacity-100 transition-opacity duration-300 flex flex-col justify-end p-6 pointer-events-none">
                        <h3 class="text-xl font-bold text-white mb-1">Personaje estilo anime</h3>
                        <p class="text-[#cbd5e1] text-sm">Modelado</p>
                    </div>
                </div>

                <!-- Tarjeta 14 (PNG/JPG) -->
                <div class="lightbox-trigger relative group rounded-2xl overflow-hidden border border-white/10 bg-[#0a0508] hover:border-[#00ff66]/50 transition-all duration-300 break-inside-avoid cursor-pointer" data-src="/3D/Tanque.png">
                    <img src="/3D/Tanque.png" alt="Pyra" class="w-full h-auto object-cover group-hover:scale-105 transition-transform duration-500" />
                    <div class="absolute inset-0 bg-[#0a0508]/80 opacity-0 group-hover:opacity-100 transition-opacity duration-300 flex flex-col justify-end p-6 pointer-events-none">
                        <h3 class="text-xl font-bold text-white mb-1">Primeros modelos</h3>
                        <p class="text-[#cbd5e1] text-sm">Modelado</p>
                    </div>
                </div>
                
                <!-- Tarjeta 15 (WEBM/MP4) -->
                <div class="lightbox-trigger relative group rounded-2xl overflow-hidden border border-white/10 bg-[#0a0508] hover:border-[#00ff66]/50 transition-all duration-300 break-inside-avoid cursor-pointer" data-src="/3D/FlowerCrystal.mp4">
                    <!-- Es vital incluir autoplay, loop, muted y playsinline para que actúe como un GIF -->
                    <video src="/3D/FlowerCrystal.mp4" autoplay loop muted playsinline class="w-full h-auto object-cover group-hover:scale-105 transition-transform duration-500"></video>
                    <div class="absolute inset-0 bg-[#0a0508]/80 opacity-0 group-hover:opacity-100 transition-opacity duration-300 flex flex-col justify-end p-6 pointer-events-none">
                        <h3 class="text-xl font-bold text-white mb-1">Material y Nodos de Geometría</h3>
                        <p class="text-[#cbd5e1] text-sm">Procedural + Materiales</p>
                    </div>
                </div>

                <!-- Tarjeta 16 (WEBM/MP4) -->
                <div class="lightbox-trigger relative group rounded-2xl overflow-hidden border border-white/10 bg-[#0a0508] hover:border-[#00ff66]/50 transition-all duration-300 break-inside-avoid cursor-pointer" data-src="/3D/Horse.webm">
                    <!-- Es vital incluir autoplay, loop, muted y playsinline para que actúe como un GIF -->
                    <video src="/3D/Horse.webm" autoplay loop muted playsinline class="w-full h-auto object-cover group-hover:scale-105 transition-transform duration-500"></video>
                    <div class="absolute inset-0 bg-[#0a0508]/80 opacity-0 group-hover:opacity-100 transition-opacity duration-300 flex flex-col justify-end p-6 pointer-events-none">
                        <h3 class="text-xl font-bold text-white mb-1">Material estilo cristal</h3>
                        <p class="text-[#cbd5e1] text-sm">Materiales + Modelado</p>
                    </div>
                </div>

                <!-- Tarjeta 17 (WEBM/MP4) -->
                <div class="lightbox-trigger relative group rounded-2xl overflow-hidden border border-white/10 bg-[#0a0508] hover:border-[#00ff66]/50 transition-all duration-300 break-inside-avoid cursor-pointer" data-src="/3D/IsometricRoom.webm">
                    <!-- Es vital incluir autoplay, loop, muted y playsinline para que actúe como un GIF -->
                    <video src="/3D/IsometricRoom.webm" autoplay loop muted playsinline class="w-full h-auto object-cover group-hover:scale-105 transition-transform duration-500"></video>
                    <div class="absolute inset-0 bg-[#0a0508]/80 opacity-0 group-hover:opacity-100 transition-opacity duration-300 flex flex-col justify-end p-6 pointer-events-none">
                        <h3 class="text-xl font-bold text-white mb-1">Habitación Isométrica</h3>
                        <p class="text-[#cbd5e1] text-sm">Modelado</p>
                    </div>
                </div>
                <!-- Pega más tarjetas aquí siguiendo el modelo de Imagen o Vídeo según necesites -->

            </div>
        </div>
    </section>

    <Contacto />

    <!-- ========================================= -->
    <!-- MODAL / LIGHTBOX (Soporte Imagen y Vídeo) -->
    <!-- ========================================= -->
    <div id="lightbox" class="fixed inset-0 z-[100] hidden bg-[#0a0508]/75 backdrop-blur-sm flex items-center justify-center p-4 opacity-0 transition-opacity duration-300">
        
        <!-- Controles de Zoom -->
        <div class="absolute top-6 left-6 md:top-10 md:left-10 flex gap-2 z-50 bg-[#0a0508]/80 p-1.5 rounded-xl border border-white/10 backdrop-blur-md">
            <button id="zoom-out" class="text-white hover:text-[#00ff66] w-8 h-8 flex items-center justify-center text-2xl font-bold transition-colors cursor-pointer select-none rounded hover:bg-white/10">-</button>
            <button id="zoom-reset" class="text-white/80 hover:text-white w-10 flex items-center justify-center text-sm font-medium transition-colors cursor-pointer select-none rounded hover:bg-white/10">1x</button>
            <button id="zoom-in" class="text-white hover:text-[#00ff66] w-8 h-8 flex items-center justify-center text-2xl font-bold transition-colors cursor-pointer select-none rounded hover:bg-white/10">+</button>
        </div>

        <button id="lightbox-close" class="absolute top-6 right-6 md:top-10 md:right-10 text-white/50 hover:text-[#00ff66] text-5xl font-light z-50 transition-colors cursor-pointer">&times;</button>
        
        <button id="lightbox-prev" class="absolute left-2 md:left-10 top-1/2 -translate-y-1/2 text-white/50 hover:text-[#00ff66] z-50 transition-colors cursor-pointer select-none p-4">
            <svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="15 18 9 12 15 6"></polyline></svg>
        </button>

        <!-- Contenedores maximizados (Uno para IMG y otro para VIDEO) -->
        <img id="lightbox-img" src="" alt="Arte maximizado" class="hidden max-w-full max-h-[90vh] object-contain drop-shadow-[0_0_50px_rgba(0,255,102,0.15)] scale-95 transition-transform duration-150 z-40 will-change-transform" />
        
        <video id="lightbox-video" src="" autoplay loop muted playsinline class="hidden max-w-full max-h-[90vh] object-contain drop-shadow-[0_0_50px_rgba(0,255,102,0.15)] scale-95 transition-transform duration-150 z-40 will-change-transform"></video>

        <button id="lightbox-next" class="absolute right-2 md:right-10 top-1/2 -translate-y-1/2 text-white/50 hover:text-[#00ff66] z-50 transition-colors cursor-pointer select-none p-4">
            <svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="9 18 15 12 9 6"></polyline></svg>
        </button>

    </div>
</Layout>

<!-- ========================================= -->
<!-- LÓGICA JAVASCRIPT PARA EL LIGHTBOX        -->
<!-- ========================================= -->
<script>
    // 1. OBTENEMOS TODOS LOS ELEMENTOS DEL DOM
    const triggers = Array.from(document.querySelectorAll('.lightbox-trigger'));
    const lightbox = document.getElementById('lightbox');
    const lightboxImg = document.getElementById('lightbox-img') as HTMLImageElement;
    const lightboxVideo = document.getElementById('lightbox-video') as HTMLVideoElement;
    const closeBtn = document.getElementById('lightbox-close');
    const prevBtn = document.getElementById('lightbox-prev');
    const nextBtn = document.getElementById('lightbox-next');
    
    // Botones de zoom
    const zoomInBtn = document.getElementById('zoom-in');
    const zoomOutBtn = document.getElementById('zoom-out');
    const zoomResetBtn = document.getElementById('zoom-reset');

    // 2. VARIABLES DE ESTADO
    let currentIndex = 0;
    let currentZoom = 1; // 1 = 100% (Tamaño original)

    if (lightbox && lightboxImg && lightboxVideo) {
        
        // --- FUNCIONES DE ZOOM ---
        
        // Aplica el nivel de zoom actual a la imagen o vídeo
        const applyZoom = () => {
            lightboxImg.style.transform = `scale(${currentZoom})`;
            lightboxVideo.style.transform = `scale(${currentZoom})`;
            if (zoomResetBtn) zoomResetBtn.innerText = `${Math.round(currentZoom * 10) / 10}x`;
        };

        // Resetea el zoom al estado original al cambiar de foto o cerrar
        const resetZoom = () => {
            currentZoom = 1;
            lightboxImg.style.transform = ''; 
            lightboxVideo.style.transform = ''; 
            if (zoomResetBtn) zoomResetBtn.innerText = '1x';
            lightboxImg.classList.add('scale-100'); 
            lightboxVideo.classList.add('scale-100'); 
        };

        // Evento: Zoom con la rueda del ratón
        lightbox.addEventListener('wheel', (e) => {
            if (!lightbox.classList.contains('hidden')) {
                e.preventDefault(); // Evita que la web haga scroll por detrás
                if (e.deltaY < 0) {
                    currentZoom = Math.min(currentZoom + 0.25, 4); // Límite máximo: 4x
                } else {
                    currentZoom = Math.max(currentZoom - 0.25, 0.5); // Límite mínimo: 0.5x
                }
                applyZoom();
            }
        });

        // Eventos: Zoom con los botones de la interfaz
        zoomInBtn?.addEventListener('click', (e) => {
            e.stopPropagation();
            currentZoom = Math.min(currentZoom + 0.5, 4);
            applyZoom();
        });
        
        zoomOutBtn?.addEventListener('click', (e) => {
            e.stopPropagation();
            currentZoom = Math.max(currentZoom - 0.5, 0.5);
            applyZoom();
        });

        zoomResetBtn?.addEventListener('click', (e) => {
            e.stopPropagation();
            resetZoom();
        });

        // --- NUEVO: FUNCIONES DE ZOOM TÁCTIL (MÓVILES) ---
        let initialDistance = 0;
        let initialZoom = 1;

        lightbox.addEventListener('touchstart', (e) => {
            // Si el usuario toca la pantalla exactamente con dos dedos
            if (e.touches.length === 2 && !lightbox.classList.contains('hidden')) {
                // Calculamos la distancia inicial entre los dedos (hipotenusa)
                initialDistance = Math.hypot(
                    e.touches[0].clientX - e.touches[1].clientX,
                    e.touches[0].clientY - e.touches[1].clientY
                );
                initialZoom = currentZoom;
            }
        });

        lightbox.addEventListener('touchmove', (e) => {
            // Mientras los dedos se mueven por la pantalla
            if (e.touches.length === 2 && !lightbox.classList.contains('hidden')) {
                e.preventDefault(); // Bloquea el zoom nativo del navegador móvil
                
                // Calculamos la nueva distancia
                const currentDistance = Math.hypot(
                    e.touches[0].clientX - e.touches[1].clientX,
                    e.touches[0].clientY - e.touches[1].clientY
                );
                
                // Aplicamos la proporción (si se alejan aumenta, si se juntan disminuye)
                const zoomFactor = currentDistance / initialDistance;
                currentZoom = Math.min(Math.max(initialZoom * zoomFactor, 0.5), 4);
                applyZoom();
            }
        }, { passive: false }); // Permite que funcione el preventDefault() en touchmove


        // --- FUNCIONES DE GALERÍA Y MULTIMEDIA ---
        
        // Carga el archivo y decide si debe usar <img> o <video>
        const showMedia = (index) => {
            const src = triggers[index].getAttribute('data-src');
            if (src) {
                resetZoom(); 
                
                const isVideo = src.endsWith('.webm') || src.endsWith('.mp4');
                
                if (isVideo) {
                    lightboxImg.classList.add('hidden');
                    lightboxImg.src = '';
                    
                    lightboxVideo.src = src;
                    lightboxVideo.classList.remove('hidden');
                    lightboxVideo.play();
                } else {
                    lightboxVideo.classList.add('hidden');
                    lightboxVideo.pause();
                    lightboxVideo.src = '';
                    
                    lightboxImg.src = src;
                    lightboxImg.classList.remove('hidden');
                }
            }
        };

        // Evento: Abrir el modal al hacer click en una tarjeta
        triggers.forEach((trigger, index) => {
            trigger.addEventListener('click', () => {
                currentIndex = index;
                showMedia(currentIndex);
                
                lightbox.classList.remove('hidden');
                setTimeout(() => {
                    lightbox.classList.remove('opacity-0');
                    lightboxImg.classList.remove('scale-95');
                    lightboxVideo.classList.remove('scale-95');
                    lightboxImg.classList.add('scale-100');
                    lightboxVideo.classList.add('scale-100');
                }, 10);
                
                document.body.style.overflow = 'hidden'; // Evita scroll en la página de fondo
            });
        });

        // Controles Anterior / Siguiente
        const goNext = () => {
            currentIndex = (currentIndex + 1) % triggers.length;
            showMedia(currentIndex);
        };

        const goPrev = () => {
            currentIndex = (currentIndex - 1 + triggers.length) % triggers.length;
            showMedia(currentIndex);
        };

        nextBtn?.addEventListener('click', (e) => { e.stopPropagation(); goNext(); });
        prevBtn?.addEventListener('click', (e) => { e.stopPropagation(); goPrev(); });

        // Evento: Cerrar el modal
        const closeLightbox = () => {
            resetZoom(); 
            lightbox.classList.add('opacity-0');
            lightboxImg.classList.remove('scale-100');
            lightboxVideo.classList.remove('scale-100');
            lightboxImg.classList.add('scale-95');
            lightboxVideo.classList.add('scale-95');
            
            // Retraso para dejar que la transición visual termine
            setTimeout(() => {
                lightbox.classList.add('hidden');
                lightboxImg.src = ''; 
                lightboxVideo.src = ''; 
                document.body.style.overflow = ''; // Devuelve el scroll a la página
            }, 300); 
        };

        // Cerrar al hacer click en la X o en el fondo oscuro
        closeBtn?.addEventListener('click', closeLightbox);
        
        lightbox.addEventListener('click', (e) => {
            if (e.target === lightbox) closeLightbox();
        });
        
        // Atajos de Teclado
        document.addEventListener('keydown', (e) => {
            if (!lightbox.classList.contains('hidden')) {
                if (e.key === 'Escape') closeLightbox();
                if (e.key === 'ArrowRight') goNext();
                if (e.key === 'ArrowLeft') goPrev();
                if (e.key === '+') { currentZoom = Math.min(currentZoom + 0.5, 4); applyZoom(); }
                if (e.key === '-') { currentZoom = Math.max(currentZoom - 0.5, 0.5); applyZoom(); }
            }
        });
    }
</script>